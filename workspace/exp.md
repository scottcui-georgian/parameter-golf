# Experiment log

## Hypothesis

(What you expect to change in `val_bpb` and why.)

## Reasoning

(Math, parameter counts, or conceptual argument.)

## References

(Papers or prior runs; N/A if none.)

## Changes

- File: `workspace/train_gpt.py`
- Summary: (what you edited.)

## Base

- Parent commit:
- Baseline `val_bpb` (if known):

---

## Results

(Fill after `python3 run.py train`. Include final `val_bpb` from the
`final_int8_zlib_roundtrip` log lines, peak VRAM from `peak memory allocated`,
submission size lines if relevant, and status: keep / discard / crash / timeout.)
