# Derived from the Parameter Golf task runtime used by arc.
# Base task deps come from task `pyproject.toml`; arc's Modal image also
# installs `zstandard` and `brotli` for compressed data / artifacts.
# FlashAttention 3 is installed separately for Hopper/H100 runs.
huggingface-hub>=0.30.0
numpy>=2.2.0
sentencepiece>=0.2.0
torch==2.9.1
zstandard
brotli

# Install separately on Hopper/H100:
# flash_attn_3 --find-links https://windreamer.github.io/flash-attention3-wheels/cu128_torch291
